#include "main.h"
/**
* _islower -Checks for lowercase alphabet
*@m is haracter to be checked
* Return:if the character is lowercase
*/
int _islower(int m)
{
if (m >= 'a' && m <= 'z')
return (1);
else 
return (0);
}
