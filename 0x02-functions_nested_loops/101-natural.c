#include <stdio.h>
/**
*main - printsthe sum of all multiples of 3 or 5 up to 1024
*Return: Always (Sucess)
*/
int main(void)
{
int b, w = 0;
while (b < 1024)
{
if (( m % 3 == 0) || (w % 5 == 0))
{
w += b;
}
b++;
}
printf("%d\n" , w);
return (0);
}
